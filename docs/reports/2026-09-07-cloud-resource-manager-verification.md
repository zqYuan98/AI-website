# 线上资源管理验收

日期：2026-09-07

## 已实现

- Neon PostgreSQL 适配与 Better Auth 唯一所有者登录，保留现有管理界面和已确认字号。
- 静态管理入口、登录/退出、受控密码恢复、数据库持久限流、服务端身份检查。
- 私有草稿、独立公开快照、版本冲突检查、事务发布、缓存失效和单独重试刷新。
- 显式来源迁移、私有备份恢复和专用只读公开连接；正式私有路由在未启用时关闭。

## 验证结果

- `node scripts/check-quality.mjs --build`：通过，包括现有内容、本机资源库回归、搜索、交互、ESLint、TypeScript 和 Next.js 正式构建。
- `scripts/cloud-auth-check.mjs`：真实 Better Auth handler + PGlite PostgreSQL 引擎；正确/错误密码、固定 owner、拒绝其他账号、关闭注册、CSRF、Cookie、过期/退出/重置撤销会话及跨实例限流通过。
- `scripts/cloud-library-check.mjs`：PGlite 中执行实际 SQL/事务；274 条既有公开资源、陈旧版本与并发冲突、普通保存不发布、白名单、回滚、导入撤回、私有恢复、零条公开快照以及私有/认证表权限隔离通过。
- 最终迁移审查后补充并通过回归：公开初始化来源必须显式指定；私人备份、混合 private/inbox 条目、额外顶层字段或未知资源字段均拒绝，不会通过剥离私人标记变成公开内容；合法空快照仍可初始化。
- `scripts/check-cloud-api.mjs`：实际路由配隔离依赖，验证访问门、同源、请求大小、错误不泄漏凭据、发布后缓存失败及幂等刷新，以及云端为空/报错不回退文件。
- `node scripts/check-smoke.mjs http://127.0.0.1:4301`：本机正式构建 13 个公开页面及登录页返回 200；未配置生产管理页、本机 API 和云库匿名读取保持关闭。
- 前轮本机浏览器检查：桌面管理页与 390px 手机入口/管理页/添加表单无横向溢出，搜索和下拉字号 16px，无页面错误提示；确认未启用的正式构建显示「私人资源库尚未启用」。该轮没有创建测试收藏或改动真实私有库内容。
- 首次账号初始化补充回归：使用 Better Auth 同版本公开输入契约校验邮箱；无效邮箱不写入用户、密码账号或会话，首尾空白和大小写规范化后的邮箱可通过实际登录。认证回归和对应 ESLint 通过。
- Neon 权限兼容性修正：角色创建与更新拆开，拒绝接管高权限、跨数据库或异常成员角色；提交前按目标角色检查最小授权，提交后通过独立公开连接验证。PGlite 中用非超级用户 CREATEROLE 身份执行首次创建和重复配置均通过，并确认旧的 `SET ROLE`、`ALTER ... NOSUPERUSER` 会报权限不足。CLI 另校验直连与运行地址属于同分支、数据库和私有角色。
- 已在独立 Neon Free 项目 `vitamin-resource-library-verify` 完成真实 pg 认证与资源库检查：账号、会话、恢复、持久限流、事务、保存不发布、发布回滚、空快照和实际公开连接权限隔离均通过。认证与资源库使用不同数据库，没有在正式库运行测试。公开 ACL 负向检查使用固定连接关闭默认只读后仍被权限系统拒绝，避免 Pool 在错误后重建连接干扰断言。
- 正式数据库的 TLS 1.3 连接及证书校验通过；认证表、私人草稿表、公开快照表、独立公开连接均已准备并验证。保留线上已生效的 274 条公开资源作为初始基线，草稿版本为 1，未迁移或发布其他私人收藏。
- Vercel 的数据库连接、认证密钥与所有者 ID 均已配置在 Production，并标为 Sensitive；只读连接变量统一为 `LIBRARY_PUBLIC_DATABASE_URL`，避开平台公开前缀限制。完整检查与正式云配置的 Next.js 构建通过。
- 本轮在隔离 Neon 数据库及本机真实 Next.js 服务完成云端浏览器流程：管理入口跳转登录；测试所有者登录成功；新增资源默认私有；整理为公开候选并保存后公开页仍为 0 条；预览确认后公开页出现 1 条；私人备注不进入预览或公开页面；改回私有后明确显示待撤下，确认发布后公开页恢复 0 条；退出后管理入口再次要求登录。管理和公开页面控制台没有 error。
- 修复实际浏览器发现的 Next.js 内部请求 URL 归一化问题：用固定配置校验合法 Host，并保留精确 POST Origin、跨站拒绝和接口白名单。补充内部 URL + 正确 Host 的实际 handler 登录/会话/退出正例，以及错误或非法 Host、伪造转发头、缺失 Origin 和跨站反例，全部通过。

## 实际开通状态

用户已亲自确认 Neon 条款，并明确授权本机 Vercel CLI；正式数据库 `vitamin-resource-library` 已建立，使用 Free 方案、iad1 区域，Neon 托管 Auth 关闭。数据库仅连接现有 `ai-website` 的 Production；隔离测试项目曾临时连接 Development，完成全部测试后已显式断开并删除，未操作正式资源。

用户已在本机受控终端成功创建正式账号；独立只读核对总用户、固定 owner、非空密码 credential、owner 草稿、公开快照均为 1，公开资源仍为 274 条。密码未通过聊天收集。`RESOURCE_LIBRARY_MODE=cloud` 已设置到 Production，代码已推送 main。没有配置或发送恢复邮件。

上线前通过实际 Vercel 部署 API 核对旧正式部署为 `5664ec418f35fb0d18023af35f738d363d9f27d1`，可作为代码回退版本。正式认证地址采用 `https://www.notvitamin.com`。

账号初始化曾失败但通用错误无法区分原因；CLI 现仅展示固定白名单中的输入或账号状态错误，密码长度立即校验，未知异常继续隐藏。非法密码不写入任何认证记录、数据库诊断不泄漏及完整认证回归通过。

代码版本 `e232ca56788650b6334a1ab241265144b894e92c` 的 [GitHub Quality](https://github.com/zqYuan98/AI-website/actions/runs/34124909029) 全部通过。Vercel 正式部署 `dpl_ER3vdoc2JrHPuo4e4KxQSsnWpXXr` 为 READY，部署 API 确认 `www.notvitamin.com` 已指向该版本，从创建到就绪约 178 秒。

正式域名验证结果：

- `node scripts/check-smoke.mjs https://www.notvitamin.com --cloud` 通过：13 个公开页面及登录页正常，匿名管理入口跳转 `/login`，匿名云库读取被拒绝，本机维护 API 的所有检查方法均关闭。
- 浏览器显示 274 条公开资源、4 个精选工具和 6 篇阅读资源；实际点击「管理资源」进入登录页，页面正常，控制台未发现 error。
- 正式认证请求验证通过：空会话返回 null 且 no-store，固定站点来源可进入密码校验，不存在的测试账号返回 401，无 Origin 请求返回 403，公开注册返回 404。未使用或读取所有者密码。
- 新部署上线后查询最近 15 分钟的 error 级运行日志，结果为空；这是本次验收窗口的观察，不代表持续监控。

实际所有者的首次线上登录由本人使用刚设置的密码完成；本轮没有将该登录或跨设备访问标记为已验证。新增资源默认私有，确认发布才更新网站。管理入口为 [我的资源库](https://www.notvitamin.com/tools/manage)。

配置与恢复步骤见 [线上管理台配置文档](../cloud-resource-manager-setup.md)。
